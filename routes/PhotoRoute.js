var express = require('express');
var router = express.Router();
var PhotoController = require('../controllers/PhotoController');

// router.post('/createPhoto', PhotoController.create);
// router.post('/getAllByUserId', PhotoController.getAllByUId);
// router.post('/removePhoto', PhotoController.remove);
// router.post('/editPhoto', PhotoController.edit);

module.exports = router;
